{
  "name": "vilawave.com",
  "version": "0.1.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "pre-publish": "rm -rf node_modules/.cache && rm -rf out && next build && next export",
    "publish": "node publish.mjs",
    "deploy": "npm run pre-publish && npm run publish"
  },
  "dependencies": {
    "next": "latest",
    "normalize.css": "^8.0.1",
    "react": "^16.7.0",
    "react-dom": "^16.7.0"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.6.0",
    "@types/react": "^16.9.46",
    "babel-plugin-transform-define": "^1.3.0",
    "gh-pages": "^3.1.0",
    "typescript": "^3.9.7"
  }
}
